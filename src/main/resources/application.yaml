spring:
  application:
    name: spring-is-cool
  main:
    keep-alive: true

spring-is-cool:
  ssh:
    enabled: true
    host: 127.0.0.1
    port: 2222
    demo-user: demo
    demo-password: demo
    host-key-path: build/ssh/hostkey.ser
  ai:
    enabled: false
    provider: static
    project: ${GOOGLE_CLOUD_PROJECT:}
    location: ${GOOGLE_CLOUD_LOCATION:global}
    model-id: ${VERTEX_AI_MODEL_ID:gemini-2.5-flash-lite}
    monthly-call-limit: ${SPRING_IS_COOL_AI_MONTHLY_CALL_LIMIT:250}
  persistence:
    enabled: true
    type: sqlite
    sqlite-path: ${SPRING_IS_COOL_PERSISTENCE_SQLITE_PATH:build/data/world.sqlite}
    session-id: ${SPRING_IS_COOL_PERSISTENCE_SESSION_ID:default-office}
